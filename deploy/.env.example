# API
DATABASE_URL=root:yourpassword@tcp(db:3306)/ai_calories?charset=utf8mb4&parseTime=True&loc=Local
JWT_SECRET=change-me
OPENAI_TOKEN=sk-...
AI_MODEL_TEXT=gpt-4o
GOOGLE_CLIENT_ID=
API_PORT=8080

# MySQL
MYSQL_ROOT_PASSWORD=yourpassword
MYSQL_DATABASE=ai_calories

# Caddy — set to your domain for auto-HTTPS, or :80 for plain HTTP
DOMAIN=:80
