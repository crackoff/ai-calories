# Go API
DATABASE_URL=root:your_password@tcp(db:3306)/ai_calories
JWT_SECRET=change-this-to-a-random-string
OPENAI_TOKEN=sk-...
AI_MODEL_TEXT=gpt-4o
GOOGLE_CLIENT_ID=
API_PORT=8080

# MySQL
MYSQL_ROOT_PASSWORD=your_password
MYSQL_DATABASE=ai_calories

# Domain — set your real domain for automatic HTTPS via Caddy
# Use "localhost" for local dev (Caddy will serve HTTP only)
DOMAIN=localhost
