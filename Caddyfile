{$DOMAIN:localhost} {
	# API backend
	handle /api/* {
		reverse_proxy api:8080
	}

	# Web frontend
	handle {
		reverse_proxy web:80
	}
}

# Direct API access on :8080 for mobile app testing
:8080 {
	reverse_proxy api:8080
}
